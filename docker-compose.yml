# Please run with `docker-compose up -d --build` command
networks:
  form-portal:
    external: true

services:
  backend:
    build: .
    # ports:
    #   - "3000:3000"
    volumes:
      - ./uploads:/workspace/${LOCAL_UPLOADS_DEST:-uploads}
    env_file:
      - .env.production
    networks:
      - form-portal